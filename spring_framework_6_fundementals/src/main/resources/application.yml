## YAML Template.
---
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/
    username: user
    password: pass
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always